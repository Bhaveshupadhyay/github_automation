{
  "$schema": "../../qa-contract.schema.json",
  "serviceType": "backend",
  "port": 8000,
  "healthCheckUrl": "/health",
  "lifecycle": {
    "prepare": "uv sync && uv run alembic upgrade head",
    "start": "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
