/**
 * Enum object for Task Intent Types.
 * Prevents string typos and establishes a single source of truth for intent classification.
 */
export const IntentType = Object.freeze({
  CODE_DEVELOPMENT: "CODE_DEVELOPMENT",
  CLARIFICATION_NEEDED: "CLARIFICATION_NEEDED"
});
