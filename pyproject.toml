[project]
name = "github-automation"
version = "0.1.0"
description = "Antigravity Autonomous Developer Workflow Engine"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
    "google-genai>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
